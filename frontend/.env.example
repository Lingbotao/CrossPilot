# 前端构建期变量（Vite 只会把 VITE_ 前缀的变量注入产物）
#
# ⚠️ 这些是**构建期**变量：改完必须重新构建（`npm run build` 或重建镜像），
#    只改运行时环境变量对已构建的产物无效。
#
# 本机开发（make web）：复制为 frontend/.env.development 即可。
# 容器部署：由 docker-compose 的 build.args 注入，不需要这个文件。

# 后端 API 前缀。生产走 nginx 反代同源，保持默认即可
VITE_API_BASE_URL=/api/v1

VITE_APP_TITLE=CrossPilot

# 仅开发用：vite dev server 把 /api 代理到哪个后端
VITE_DEV_API_TARGET=http://localhost:8000
