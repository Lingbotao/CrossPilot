"""平台 Webhook 接收层（M2 落地）。

约定（PRD 8.4 / 约束）：
1. **必须先验签再解析** —— 各平台签名算法不同，验签逻辑放各自适配器；
2. Webhook 与轮询是**双通道**，同一条数据可能重复到达 →
   靠 ``{platform}:{shop_id}:{platform_order_id}`` 幂等键 + DB 唯一索引去重；
3. Webhook 只做「入队 + 快速返回 200」，重活交给 Celery ——
   平台对响应超时有严格要求，在 webhook 里同步处理必然导致平台重推与超时告警；
4. 未验签通过的请求**直接 401 且不入库**，并记录来源 IP 与原始报文哈希（防重放）。
"""

__all__: list[str] = []
